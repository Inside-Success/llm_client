# LLM Client Concern Register

Wiki home: http://localhost:8088/index.php/Project_Wiki

## Open Concerns

| ID | Concern | Severity | Current mitigation | Next evidence/action |
|---|---|---:|---|---|
| LLM-PORT-001 | Infrastructure breadth can obscure portfolio value. | High | Applied observability case frames traces as decision evidence. | Publish one downstream trace with a clear before/after decision. |
| LLM-PORT-002 | Observability can be mistaken for analytic proof. | High | Validation register separates runtime, trace, decision, and analysis validity. | Tie claims to downstream project artifacts. |
| LLM-PORT-003 | Prompt-evaluation logic can drift back into this repo. | Medium | Capability decomposition assigns prompt-eval semantics to `prompt_eval`. | Keep new evaluation features behind explicit boundary decisions. |
| LLM-PORT-004 | Runtime substrate can bloat into workflow orchestration. | Medium | ADR 0010 keeps workflow orchestration above this layer. | Use separate workflow runtime when durable orchestration is required. |
| LLM-PORT-005 | Model/provider behavior changes over time. | Medium | Model registry, trace logging, and errors/cost queries expose drift. | Use empirical traces before changing task policy. |
| LLM-RUNTIME-006 | Structured callers could not require native JSON schema; provider schema rejection silently changed execution to Instructor, whose internal retries are separate from `RetryPolicy`. | High | Strict routing and exact replay are implemented. After two rejected repairs, fresh independent review accepted `5ed2a1e` with real four-path capture-to-replay and the full adversarial matrix. | Require Plan 0141 to pin the implementation and acceptance-evidence commits and pass its downstream replay before runtime authorization. |
| LLM-VERIFY-007 | The non-E2E suite appeared stalled, and the later completion record masked a separate declared-test integrity failure. | High | Timeout/cooldown diagnostics are implemented; AST node resolution and exact declarations make the canonical-venv `check_plan_tests.py --plan 99` execute 320 tests and exit zero after integration with current main (the accepted `5ed2a1e` boundary executed 375 under its recorded environment). | Keep the command and exact interpreter in independent acceptance and plan completion so registry or environment drift cannot recur. |
| LLM-VERIFY-008 | A June backup merge restored pre-Plan-32 tool-decorator production code while retaining the accepted sync/registry/type tests, leaving seven failures on `origin/main`. | High | Restored accepted Plans 32/47 sync support, `ToolRegistry.has`, and concrete annotation metadata while preserving later goal/complexity/result-type fields; 38 decorator/public-surface tests pass. | Keep the full completion suite green and prevent unverified backup merges from replacing newer implementation with older snapshots. |
| LLM-VERIFY-009 | The first Plan 99 replay repair was called locally exact after policy-field tests, but independent review found that the surrounding snapshot envelope could shed or reinterpret that authority and still dispatch. | High | Added TDD attacks for all-version stored-fingerprint mismatch, full v2-to-v1 downgrade, missing/malformed support metadata, API/kind reinterpretation, unknown envelope/control fields, non-replayable message values, schema drift, and omitted text execution mode; genuine v1 remains a positive control. | For replay/versioned envelopes, test the full state-transition matrix (integrity, downgrade, metadata deletion, kind substitution, unknown fields, and every captured control) rather than only fields inside the new policy object. |
| LLM-VERIFY-010 | The second Plan 99 repair was called locally green after real public-path capture tests and builder-level replay tests, but no test fed each real captured snapshot into replay. The producer's timeout-disabled sentinel and lossy normalization therefore disagreed with the consumer while all focused tests passed. | High | Third repair uses capture-to-replay tests for all four public APIs and intrinsic unsupported-value markers for every non-JSON-native coercion; independent review accepted exact commit `5ed2a1e`. | Preserve producer-to-actual-consumer round trips as mandatory evidence for persisted contracts; field inspection and hand-built consumer fixtures remain supporting evidence only. |
| LLM-VERIFY-011 | The repository-wide `make check` lint gate is red on clean `origin/main` with 315 Ruff violations, so it cannot currently certify otherwise-green increments. | High | Plan 97 records the exact inherited baseline; its changed modules are Ruff-clean and 365 focused/wider regression tests pass. | Land a dedicated lint-baseline repair or scoped-baseline gate, then require `make check` on every merge; do not describe the current default branch as fully green. |
| LLM-VERIFY-012 | The venv-backed full suite can segfault when a lifecycle heartbeat thread writes through the shared SQLite connection while a later test fixture closes or replaces that connection. | High | Reproduced on clean `origin/main` and the Plan 99 integration tree with the same `_run_db_write` / fixture-teardown stack; isolated lifecycle and Plan 99/97 overlap tests remain green. | Give DB connection replacement the same write-lock ordering as `_run_db_write`, migrate direct-global fixtures to a safe isolation seam, and add a repeated teardown-vs-writer negative before claiming the full suite green. |
| LLM-VERIFY-013 | Worktree verification escaped its declared environment: the pre-commit hook could fall back to ambient Python, suppress generator/fetch failures, and the mandatory plan-test helper launched ambient `pytest` even when invoked through the canonical venv. Generated docs and test evidence could therefore look green under a different interpreter than the command selected. | High | The hook now requires and reports a worktree or canonical-repo venv and fails if generation, staging, coupling, typing, plan-status, or freshness fetch fails; the plan-test helper launches `sys.executable -m pytest` and has a command-construction regression test. | Every verification launcher must bind all child Python tools to the selected interpreter, include a negative for environment escape, and report exact interpreter/tool versions with any baseline count. |
| LLM-VERIFY-014 | **Resolved 2026-08-03.** The repository had `AGENTS.md -> CLAUDE.md` while `CLAUDE.md` identified itself as a generated projection of itself. | Medium | Restored `CLAUDE.md` as authored authority, rendered a separate regular `AGENTS.md`, and verified deterministic sync. | Keep the symlink-overwrite negative control and require `check_agents_sync.py --check` after instruction-source changes. |
| LLM-VERIFY-015 | Repository-wide strict mypy is red on unrelated baseline modules, so import-following checks cannot certify otherwise clean runtime increments. | High | Plan 97's changed modules emit no direct mypy diagnostics; focused Ruff, 174 structured/observability tests, and the 1,636-test repository suite pass. | Repair the baseline typing errors or add an honest changed-surface type gate that still follows typed public seams; do not report the current full typecheck as green. |
| LLM-RUNTIME-016 | Normalized provider controls can be silently discarded by local family allowlists or by an OpenRouter provider that ignores unsupported parameters, while vendor telemetry can be mistaken for a replacement for cross-provider execution evidence. | High | Plan 110 forwards normalized reasoning controls generically, requires parameter-capable OpenRouter routes, projects trace identity to Broadcast, keeps local evidence authoritative, and proves exact max-effort transport through installed LiteLLM without a paid call. | Preserve fail-loud capability handling and keep local evidence authoritative. |
| LLM-POLICY-017 | **Resolved 2026-07-22.** An Opus ban could be bypassed through workspace-agent aliases, registries, workflow defaults, fallback legs, provider model arrays, or opaque OpenRouter Auto Router/preset selection. | High | Plan 110 hard-blocks every resolved leg before dispatch, rejects opaque account-side selectors, inspects provider model arrays, removes aliases/registry/defaults, and audits override-aware literals. Direct, agent, fallback, Auto Router, and preset negatives pass. | Keep every pre-dispatch negative and registry/static-audit exclusion as permanent regression coverage; use account Guardrails only as defense in depth. |
| LLM-RUNTIME-018 | **Resolved 2026-07-22.** A recovered native structured call retained every attempt's lineage but returned only the selected response's price, so strict consumers could either undercount a billed schema-invalid attempt or reject the otherwise valid recovery. | Critical | Plan 111 aggregates every priceable native structured response into logical-call cost and marks exact coverage; timeouts and pre-return failures remain incomplete. The strict Cybernetic consumer accepts complete receipts and still refuses unknown spend. | Preserve sync/async repair arithmetic, timeout/pre-return negatives, and downstream fail-loud coverage checks. |

## Portfolio Judgment

`llm_client` is valuable supporting evidence for governed AI engineering. It
should not lead the CIA/analyst-facing portfolio by itself, but it can make
applied systems more credible when a reviewer can see that model behavior,
costs, errors, and structured-output failures were visible and acted on.
