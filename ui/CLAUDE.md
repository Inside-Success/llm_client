# UI surface routing

`registry.yaml` is the UI inventory authority. The existing observability
workbench is canonical; modify current source rather than creating a parallel
UI. Viewing observability data does not grant provider-call or credential authority.
